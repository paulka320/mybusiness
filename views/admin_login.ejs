<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Login | EasyMarket</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
<%- include('header') %>
<main class="section upload-section">
    <div class="section-heading">
        <h2>Admin Portal</h2>
        <p>Sign in to manage product stock, approvals, and marketplace inventory.</p>
    </div>

    <% if (locals.errors && errors.length > 0) { %>
        <div class="alert alert-error">
            <ul>
                <% errors.forEach(function(error) { %>
                    <li><%= error %></li>
                <% }); %>
            </ul>
        </div>
    <% } %>

    <form class="product-form" method="POST" action="admin_login.php">
        <label for="admin-email">Username or Email</label>
        <input type="text" id="admin-email" name="email" placeholder="Enter admin username or email" value="<%= locals.email || '' %>" required autocomplete="username">

        <div style="display: flex; justify-content: space-between; align-items: center; margin-top: 10px;">
            <label for="admin-password" style="margin: 0;">Password</label>
            <a href="forgot_password.php" style="font-size: 12px; color: #2563eb; font-weight: 600; text-decoration: none;">Forgot Password? (Get OTP)</a>
        </div>
        <div style="position: relative;">
            <input type="password" id="admin-password" name="password" required autocomplete="current-password" style="width: 100%;">
            <button type="button" id="toggle-pwd-btn" style="position: absolute; right: 10px; top: 50%; transform: translateY(-50%); background: none; border: none; cursor: pointer; color: #64748b; font-size: 12px; padding: 4px 8px;">Show</button>
        </div>

        <button type="submit" id="admin-login-submit" style="margin-top: 16px;">Login as Admin</button>
    </form>

    <div style="text-align: center; margin-top: 18px; font-size: 13px;">
        <a href="forgot_password.php" style="color: #64748b; text-decoration: none;">🔐 Reset password via registered email OTP</a>
    </div>
</main>

<script>
    const toggleBtn = document.getElementById('toggle-pwd-btn');
    const pwdInput = document.getElementById('admin-password');
    toggleBtn?.addEventListener('click', function() {
        if (pwdInput.type === 'password') {
            pwdInput.type = 'text';
            toggleBtn.textContent = 'Hide';
        } else {
            pwdInput.type = 'password';
            toggleBtn.textContent = 'Show';
        }
    });
</script>
</body>
</html>
