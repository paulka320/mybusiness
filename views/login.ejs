<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login | EasyMarket</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
<%- include('header') %>
<main class="section upload-section">
    <div class="section-heading">
        <h2>Login to EasyMarket</h2>
        <p>Access your orders and checkout faster.</p>
    </div>

    <% if (locals.errors && errors.length > 0) { %>
        <div class="alert alert-error">
            <ul>
                <% errors.forEach(function(error) { %>
                    <li><%= error %></li>
                <% }); %>
            </ul>
        </div>
    <% } %>

    <form class="product-form" method="POST" action="login.php">
        <input type="hidden" name="return" value="<%= returnUrl %>">
        <label for="login-email">Email or Username</label>
        <input type="text" id="login-email" name="email" placeholder="you@example.com or EasyMarket@admin123easymarket" value="<%= locals.email || '' %>" required autocomplete="username">

        <label for="login-password">Password</label>
        <input type="password" id="login-password" name="password" required autocomplete="current-password">

        <button type="submit" id="user-login-submit">Login</button>
    </form>

    <p>Don't have an account? <a href="register.php">Register here</a>.</p>
</main>
</body>
</html>
